# 个人本地视频后期工具：简要开发说明

## 背景

工作需要制作操作指引网页中的演示视频/动图，Screen Studio这款App完美符合需求，但这款App必须登录账号使用，因公司网络限制无法登录账号，离线使用只能处理素材和查看效果，使用无法导出后期处理过的MP4文件。

所以我希望开发一款工具/App，代替Screen Studio，完成演示视频的制作。

## 想做什么

开发一个完全本地运行的小工具：把原始屏幕录制视频和鼠标事件 JSON 加工成更清晰的操作演示视频。

期望效果：平滑鼠标、点击提示、自动缩放、MP4导出、GIF导出和关键帧PNG。

相关功能以 Screen Studio 作为参考。

## 输入与输出

- 输入：本地 MP4、自己定义的`session.json`。
- `session.json` 只需支持：视频尺寸/帧率、鼠标移动时间和坐标、点击时间和坐标、缩放起止时间/目标点/倍率。
- 输出：MP4、短 GIF、指定时间点的 PNG。
- 全流程离线运行：无登录、无账户、无云端渲染、无遥测、无上传

## 第一阶段：先做可验证的渲染器

只实现 CLI，不做桌面界面或录制功能。

```text
MP4 + seesion.json -> 渲染器 -> MP4 / GIF / PNG
```

必须具备：

1. 校验 JSON；
2. 平滑插值鼠标轨迹并叠加光标；
3. 绘制点击圆环或涟漪；
4. 按时间轴完成平滑放大、停留、还原；
5. 提供完全合成的事例视频和事件文件；
6. 为坐标映射、插值和缩放时间轴编写测试；
7. 提供 README 中的一条可复制运行命令。

推荐：Python、PyAV/OpenCV、Pillow、FFmpeg。实现应保持渲染核心和未来 UI 分离。

## 第二阶段：本地桌面界面

加入导入、预览、调整光标/点击/缩放参数、预设和导出进度。所有文件写入用户主动选择的本地目录，失败时不得修改原始文件。

## 第三阶段：录制功能

在 MacOS 上使用 ScreenCaptureKit 录制显示器、窗口或区域；
同步写入原始视频与鼠标/点击JSON；
明确申请系统权限；
录制完成后进入第二阶段的预览与渲染流程。

## 质量与限制

- 只实现通用功能，不复制任何商业软件的代码、界面、品牌或专有素材。
- 先完成第一阶段的可测试闭环，在开始 UI 或录制功能




















